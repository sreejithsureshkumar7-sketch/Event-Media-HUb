# Firebase Storage is intentionally NOT used by EventMediaHub.
# Photos and videos are stored in Cloudinary using the unsigned `eventmediahub` upload preset.
# Keep this file only as documentation; do not deploy it as Storage Rules.
